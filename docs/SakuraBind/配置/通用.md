---
sidebar_position: 1  
---

# 通用

配置文件使用 `Yaml` 语法，后缀为 `.yml`，为了能够正确配置，请自行了解[基本语法](https://www.runoob.com/w3cnote/yaml-intro.html)。

## 基本规则

YAML 对缩进敏感，推荐统一使用两个空格，不要使用 Tab。

列表写法：

```yaml
send-back-queue:
- player
- ender-chest
- database
```

层级写法：

```yaml
item-deny:
  drop: true
  click@: true
```

字符串中有特殊符号、正则表达式、颜色代码时，建议加引号：

```yaml
name-without-color: '.*传说.*'
material: '.*_SWORD'
temp-chest-title: '&a{0} 的暂存箱'
```

布尔值使用 `true` / `false`，不要写成中文。



所有配置文件都能够在**保存之后自动重载**，但是某些系统失效，比如网易的堡垒机。



此时你可以通过命令 `/sakurabind reload` 重载全部配置，或用 `/sakurabind reload <file>` 只重载某个配置文件。

部分配置修改后需要重启，例如监听器开关、模块总开关、日志文件路径和缓存大小。具体以配置注释中的“重启生效”为准。

## 修改配置的推荐流程

1. 关闭或暂停会频繁触发的功能，例如迁移模块。
2. 修改一个文件，不要一次改太多。
3. 保存后执行 `/sakurabind reload <文件名>`。
4. 用 `/sakurabind test tryMatch <匹配器ID>` 或实际物品测试。
5. 确认无误后再继续改下一个文件。

大型服建议先在测试服验证自动绑定、唯一物品、数据迁移这类会批量处理物品的功能。
